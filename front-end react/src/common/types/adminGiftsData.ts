interface GiftsData {
    giftId: string;
    giftName: string;
    giftImageUrl: string;
    giftDetails: string;
    giftPrice: number | string;
    createdAt: string;
    updatedAt: string;
}

export default GiftsData;
